# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'watsup' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'SnapKit', '~> 5.0.0'
  # Pods for watsup

  target 'watsupTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'watsupUITests' do
    # Pods for testing
  end

end
